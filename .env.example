# Local Docker Compose only. Replace both required values before starting.
POSTGRES_PASSWORD=replace-with-a-strong-local-password
SESSION_SECRET=replace-with-at-least-32-random-characters

# Optional. The deterministic demo works without paid AI credentials.
OPENAI_API_KEY=
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.5-flash
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
